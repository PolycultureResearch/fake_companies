name: fake_companies_cpg
version: "1.0.0"
config-version: 2

profile: fake_companies_cpg

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fake_companies_cpg:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
