fake_companies_cpg:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('FAKE_DB', '../../out/bristlecone.duckdb') }}"
      threads: 4
      # Raw schemas (ad_platform, erp, pos, promo, meta) already live inside
      # the same DuckDB file the generator wrote. dbt materializes its own
      # models into main / main_staging / main_marts.
